In `index.js`, write implementations for the following functions:

* `forEach()`
* `map()`
* `find()`
* `filter()`
* `reduce()`

Install the dependencies with `npm install`, then run `npm test` to test your work.
